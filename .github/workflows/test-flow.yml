name: Test Flow

on:
  push:
    branches:
      - "*"
  workflow_dispatch:
    inputs: {}
    sender: "SDP Devops Team"

jobs:
  test:
    uses: ./.github/workflows/test.yml
  lint:
    uses: ./.github/workflows/lint.yml
